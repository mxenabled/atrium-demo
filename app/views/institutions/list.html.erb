<h2>Whatever</h2>
<%= current_user.email %>
<div>
<% for @institution in @institutions %>
<div class="button">
    <img src='<%=@institution.small_logo_url %>'>
    <%= link_to "#{@institution.name}",{
        :controller => "members",
        :action => "new",
        :method => "get",
        :code => @institution.code
    } %>
    </div>
<% end %>
</div>